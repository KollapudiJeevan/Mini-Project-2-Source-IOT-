# Mini-Project-2-Source-IOT-
This ESP32 runs on WiFi and provides REST API endpoints for remote control. Users can check device health, read sensor data, enable encryption, change upload interval, and trigger instant uploads using simple terminal commands. It sends JSON data securely to a Flask server over HTTP.
